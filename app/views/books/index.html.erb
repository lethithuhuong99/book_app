<p id="notice"><%= notice %></p>

<h1 class="text-center mb-5">Books</h1>

<div class="container-fluid text-right">
  <%= link_to new_book_path  do %>
    
    <i class="fa fa-book btn btn-outline-danger"> New Book</i>

  <% end %>
</div>

<table class="table table-bordered mt-5">
  <thead class="thead-pink">
    <tr>
      <th>Title</th>
      <th>Author</th>
    <!--   <th>Description</th>
      <th>Picture</th> -->
      <th class="text-center">Edit</th>
      <th class="text-center">Delete</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><strong> <%= link_to book.title, book , style:"color:#000"%> </strong></td>
        <td><%= book.author %></td>
        <% if (user_signed_in? && (current_user.id == book.user_id)) %>
          <td>
            <%= link_to edit_book_path(book)  do %>
  
              <i class='fas fa-edit text-center container-fluid' style="color: #000"></i>

            <% end %>                         
          </td>

          <td>
            <%= link_to book, method: :delete, data: { confirm: 'Are you sure?' } do %>
              <i class='fas fa-trash text-center container-fluid' style="color: #f00"></i>
            <% end %>
            </td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


